package co.com.servientrega.domain.delivery.shipments.values;

public class Sender extends Customer {
    public Sender(String fullName, Integer age, String DNI, String address) {
        super(fullName, age, DNI, address);
    }
}
